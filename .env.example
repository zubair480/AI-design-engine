# Environment variables for local development
# (Not needed when deployed on Modal if using modal secret create)

# Optional: Override default simulation parameters
# SIM_NUM_SCENARIOS=5000
# SIM_BATCH_SIZE=50

# Optional: Override LLM model
# LLM_MODEL_ID=meta-llama/Llama-3.1-70B-Instruct

# Optional: Regional preferences
# Modal assumes US region by default
# MODAL_REGION=us-west
